# Use the official PostgreSQL 18 Alpine image as the base
FROM postgres:18-alpine

# You can add custom initialization scripts here if needed by copying them to /docker-entrypoint-initdb.d/
# COPY ./init.sql /docker-entrypoint-initdb.d/

# Expose the default PostgreSQL port
EXPOSE 5432
